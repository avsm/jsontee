* Is there a bug in the Logs META files?  Just addings `logs.cli` results in linking errors, so there arent enough dependencies in the META files.
* Port to topkg so bytecode only builds work as well
